---
name: doc-mindmap
description: 文档智能整理助手 - 批量转换办公文档为 Markdown，生成摘要与思维导图分类
---

# Doc Mindmap - 文档智能整理助手 📚🧠

将散落的办公文档（PDF、PPT、Word、Excel 等）批量转换为 Markdown，再由 Claude 生成摘要、思维导图分类和目录结构建议。

## When to Use

Use this skill when users:
- 想整理大量文档、分类归档
- 需要给一批文档生成摘要
- 想生成文档的思维导图 / mindmap
- 想把 PDF、PPT、Word 转成 Markdown
- 需要文档分类建议或目录结构方案
- 想快速了解一个文件夹里都有什么文档

触发关键词: 文档整理, 文档分类, 思维导图, mindmap, 文档摘要, PDF 转 Markdown, 批量转换, 文档归档

## Features

- **🔄 批量转换** - PDF、PPT、Word、Excel 等一键转 Markdown
- **📋 CSV 索引** - 自动生成文档索引，方便追踪
- **📝 智能摘要** - Claude 为每个文档生成简明摘要
- **🧠 思维导图** - 自动分析文档关系，生成分类思维导图
- **📂 分类方案** - 根据文档内容建议 2-3 种分类方案
- **🛡️ 安全机制** - 只读转换，不修改原始文件

## Supported Formats

| 格式 | 扩展名 | 说明 |
|------|--------|------|
| 📄 PDF | .pdf | PDF 文档 |
| 📊 PPT | .pptx | PowerPoint 演示文稿 |
| 📝 Word | .docx | Word 文档 |
| 📈 Excel | .xlsx, .xls | 电子表格 |
| 📈 CSV | .csv | 逗号分隔值 |
| 🌐 HTML | .html, .htm | 网页 |
| 📚 EPUB | .epub | 电子书 |
| 📋 JSON | .json | JSON 数据 |
| 📋 XML | .xml | XML 数据 |

## Usage

### 预览文档列表

```bash
python scripts/doc_converter.py ~/Documents/reports --preview
```

### 执行转换

```bash
python scripts/doc_converter.py ~/Documents/reports --convert --confirm
```

### 转换指定文件

```bash
python scripts/doc_converter.py file1.pdf file2.pptx --convert --confirm
```

### JSON 格式输出

```bash
python scripts/doc_converter.py ~/Documents --preview --json
```

## Arguments

| 参数 | 说明 |
|------|------|
| `paths` | 文件或目录路径（支持多个） |
| `--preview` | 预览模式，列出文档不转换 |
| `--convert` | 执行批量转换 |
| `--confirm` | 确认执行（安全机制） |
| `--json` | JSON 格式输出 |

## Output Structure

转换输出在源文件夹的 `.summaries/` 隐藏目录下：

```
{source}/
└── .summaries/
    ├── converted/              # markitdown 转换的 .md 文件
    │   ├── report.pdf.md
    │   ├── slides.pptx.md
    │   └── data.xlsx.md
    ├── briefs/                 # Claude 生成的摘要
    │   ├── report.pdf.brief.md
    │   ├── slides.pptx.brief.md
    │   └── data.xlsx.brief.md
    ├── mindmap.md              # Claude 生成的思维导图分类
    ├── index.csv               # 初始转换索引
    └── index_updated.csv       # 含分类信息的更新索引
```

## Dependencies

- Python 3.10+
- markitdown: `pip install 'markitdown[all]'`

## Claude Workflow

Claude 使用此技能时，按以下步骤执行：

### 第 1 步：预览文档

运行预览命令，向用户展示文档列表：

```bash
python doc-mindmap/scripts/doc_converter.py <路径> --preview
```

告知用户找到的文档数量、类型分布和总大小，等待确认。

### 第 2 步：执行转换

用户确认后执行转换：

```bash
python doc-mindmap/scripts/doc_converter.py <路径> --convert --confirm
```

### 第 3 步：读取转换结果

1. 读取 `.summaries/index.csv` 获取转换状态
2. 逐个读取 `.summaries/converted/` 下的 .md 文件

### 第 4 步：生成摘要

为每个成功转换的文档生成 100-200 字的中文摘要，保存到 `.summaries/briefs/` 目录：

摘要格式：
```markdown
# {文档名} 摘要

**文件类型**: {类型} | **大小**: {大小}
**核心内容**: 一句话概括

## 要点

- 要点 1
- 要点 2
- 要点 3

## 关键词

`关键词1` `关键词2` `关键词3`
```

### 第 5 步：生成思维导图分类

分析所有摘要，生成 `.summaries/mindmap.md`：

```markdown
# 📚 文档思维导图

## 🗂️ 分类概览

​```
文档集合 ({N} 个文档)
├── 📁 分类 A ({n} 个)
│   ├── 文档 1
│   └── 文档 2
├── 📁 分类 B ({n} 个)
│   └── 文档 3
└── 📁 分类 C ({n} 个)
    ├── 文档 4
    └── 文档 5
​```

## 📊 分类详情

### 📁 分类 A
- **文档 1**: 一句话摘要
- **文档 2**: 一句话摘要

...
```

### 第 6 步：建议分类方案

如果文档类型多样（>5 个），建议 2-3 种分类方案：

1. **按主题分类** - 适合研究者，按内容主题归档
2. **按用途分类** - 适合上班族，按工作场景分类（会议/汇报/参考）
3. **按时间分类** - 适合项目管理，按时间线归档

### 第 7 步：更新 CSV 索引

在 `.summaries/index_updated.csv` 中添加列：
- `summary_path`: 摘要文件路径
- `category`: 分类名称
- `tags`: 标签（逗号分隔）

### 第 8 步：展示结果

向用户展示：
1. 转换统计（成功/失败数量）
2. 思维导图分类预览
3. 推荐的分类方案
4. 后续可用操作（如按方案创建文件夹等）

## Credits

- [markitdown](https://github.com/microsoft/markitdown) - Microsoft 出品的文档转 Markdown 工具
- 与 [file-organizer](../file-organizer/) 配合使用，先分类再整理
